CFG = {};

// max records per page
CFG.MaxRecs = 30;

// Max number of pages in pagination, used for limit "skip" option in mongo
CFG.MaxPages = 30;

// scroll margin
CFG.ScrollMargin = 3;

// max comments per thread
CFG.MaxComments = 800;
